import React from "react";
import StretchText from "./StretchText";
import "../styles/StretchText.css";

const Hero = () => {
	return (
		<section className="hero-section">
			<StretchText text="Hi World! I'm Sam." />
		</section>
	);
};

export default Hero;
